<% layout('layouts/boilerplate')%>

<h1>All Campgrounds</h1>





<section class="row">
<% for (let campground of campgrounds){%>
    <div class="col-md-4">
        <div class="card mb-4 shadow-sm">
            <!-- START: Campground Image -->

                        <img class="img-fluid" alt="" src="<%=campground.images[0].url%>" >
<!--                        <img class="bd-placeholder-img card-img-top" width="100%" height="225"  src="<%=campground.images[0].url%>" preserveaspectratio="xMidYMid slice" focusable="false" role="img" aria-label="Placeholder: Thumbnail">-->

            <rect width="100%" height="100%" fill="#55595c">
                <div class="card-body">
                    <!--                <p class="card-text"><%= campground.description %></p>-->
                    <!--                <p class="card-text">-->
                    <!--                    <small class="text-muted"><%= campground.location%></small>-->
                    <!--                </p>-->
                    <!--                <a class="btn btn-primary" href="/campgrounds/<%=campground._id%>">View <%=campground.title%></a>-->
                    <div class="btn-group">
                        <form action="/campgrounds/<%=campground._id%>/like" method="GET">
                            <button class="btn btn-sm btn-secondary">
                                <i class="fas fa-thumbs-up" aria-hidden="true"></i> Like
                            </button>
                        </form>
                        <form class="inline-form" action="/campgrounds/<%=campground._id%>" method="GET">
                            <button class="btn btn-sm btn-outline-secondary"><i class="fas fa-mountain" aria-hidden="true"></i> View</button>
                        </form>
                    </div>
                    <small class="text-muted"><%= campground.like%></small>
                </div>
            </rect>
        </div>

    </div>

<% }%>
</section>